# 2026-07-09

## Enneagram CMS Import + --update-existing 支持

### 诊断
- Enneagram Hub 页面不显示内容：PR #1630 将 `EnneagramHubContentScaffold` 换为 `PublicContentAssetRenderer`，但 CMS 资产（26个 content_ready 资产）的 sections/faq 字段全为空 → 页面只剩英雄区

### CMS 导入
- 生成 PACKAGE.json 和 QA.json：从 10 个 markdown 内容草稿（1 Hub + 9 Types）提取内容
- 本地验证：draft write → promote → content_ready 全部通过
- 发现生产环境阻塞：DraftWriter 的 `existingAssetIsWritableDraft` gate 阻止更新已有 content_ready 资产

### 代码修改（fap-api PR #2821）
- `PersonalityEnneagramCmsDraft.php`：新增 `--update-existing` 选项
- `EnneagramCmsDraftWriter.php`：
  - 新增 `assetIsUpdatableContentReady()` gate
  - `--update-existing` 模式下允许更新 content_ready 资产
  - UPDATE 只改 content 字段（title/summary/sections/faq/links/seo），保留 state 字段（launch_state/is_public/robots）
- **PR #2821 merged** at e8ba73a, post-merge cleanup done, branch deleted

### 待生产执行（PR 合并部署后）
```bash
php artisan personality:enneagram-cms-draft --write --update-existing \
  --package storage/app/enneagram-package-v1.json \
  --qa storage/app/enneagram-qa-v1.json \
  --draft-only --no-publish --no-index --no-sitemap --no-llms --no-search-release \
  --operator-approved=ENNEAGRAM-CMS-DRAFT-WRITER-CONTRACT-01
```

### 待后续
- Center 页面（gut/heart/head）无内容草稿
- fap-web noindex 移除（blocker B2）
- Sitemap/LLMs enneagram 框架支持（blocker B3）

## Enneagram 全量内容资产审计 (2026-07-09)

输出: `output/ENNEAGRAM_PERSONALITY_FULL_CONTENT_ASSET_AUDIT_SOURCE_OF_TRUTH_2026-07-09.md`
- **PR #1633 merged** (ENNEAGRAM-AUDIT-SOURCE-OF-TRUTH-COMMIT-01): audit committed to `docs/seo/personality/enneagram/` as durable source-of-truth

### 核心发现
- 28 页面（26 personality + 2 test），全部 live，26 页为 thin content（~60-100字/页）
- Sitemap 0% | llms.txt 0% | llms-full.txt 0% — 全部 personality 页面不可被发现
- SitemapGenerator L326 硬编码 Big Five only（blocker B3）
- `[...slug]/page.tsx` L80 硬编码 noindex: true（blocker B2）
- 无 Enneagram Publish Gate 命令（blocker B4）
- Hub page 缺少 indexEligible gate（B6）
- zh-CN 13/13 内容包全部完成 (1 Hub + 9 Types + 3 Centers)
- en 13 页全无 CMS 资产
- Claim/Compliance: 全部安全通过
- **PR #1634 merged** (ENNEAGRAM-ZH-CENTER-CONTENT-PACKAGE-01): 3 center content drafts committed, 27 sections + 19 FAQ, ~10,700 chars

### 批次数
- Batch 0: 4 P0 技术阻塞 + 1 P1 修复
- Batch 1-3: zh 内容导入（内容已有，需跑生产命令）
- Batch 4: Center 页内容撰写 ✅ (PR #1634: 3 centers, 27 sections, 19 FAQ)
- **PR #1635 merged** (ENNEAGRAM-ZH13-CONTENT-QA-01): 13-page QA PASS — 101 sections, 56 FAQ, 0 blockers
- **fap-api PR #2824 merged** (ENNEAGRAM-ZH13-CMS-IMPORT-DRY-RUN-01): Dry-run PASS
- **生产 CMS 写入完成**: 13/13 `updated_content_ready_asset`, 前端全部 13 页 RICH content ✅
- **PR #1636 merged** (ENNEAGRAM-TYPE-CENTER-NOINDEX-GATE-REPAIR-01): B2 + B6 fixed and deployed ✅
- **fap-api PR #2826 merged** (ENNEAGRAM-CMS-PUBLISH-GATE-COMMAND-01): B4 fixed
- **fap-web PR #1637 merged** (ENNEAGRAM-LLMS-FEED-REPAIR-01): B5 fixed — llms.txt + llms-full.txt Enneagram support behind backend gates
- **All 5 blockers code-complete** ✅ B2+B6 deployed, B3+B4+B5 merged (deploy pending)
- Batch 6: en 语言 + future expansions
