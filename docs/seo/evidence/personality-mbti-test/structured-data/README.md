# Legacy Structured Data Evidence (Archive)

本目录为历史归档证据，仅用于追溯 legacy slug `personality-mbti-test` 的迁移过程。

- legacy URL（历史）：`/test/personality-mbti-test`
- canonical URL（现行）：`/tests/mbti-personality-test-16-personality-types`
- 状态：`archive-only`（不再新增）

## 使用规则

1. 本目录文件不删除，保留历史回链与索引迁移证据。
2. 新的结构化数据证据必须写入 canonical 目录：
   `docs/seo/evidence/mbti-personality-test-16-personality-types/structured-data/`
3. 任何对外规范、示例和验收命令，统一使用 `/tests/{canonical-slug}`。
