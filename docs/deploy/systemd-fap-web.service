# Example systemd unit for fap-web (Next.js)

[Unit]
Description=fap-web (Next.js)
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/fap-web
Environment=NODE_ENV=production
ExecStart=/usr/bin/env pnpm start -- -p 3000
Restart=on-failure
RestartSec=3

[Install]
WantedBy=multi-user.target
