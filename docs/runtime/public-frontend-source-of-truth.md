# Public Frontend Source of Truth Lock

Scope: PR-PRAC-01

Runtime behavior changed: no.

This document locks the current public frontend source-of-truth for FermatMind public runtime audits. It is an inventory and governance artifact only. It does not change routes, deployment scripts, sitemap output, robots output, or public rendering.

## Decision

`/Users/rainie/Desktop/GitHub/fap-web` is the primary public frontend runtime source candidate for current public product audits.

`/Users/rainie/Desktop/GitHub/fap-api/fap-web` is classified as `skeleton_or_stale`. It contains only a minimal `app/sitemap.ts` and `app/robots.ts` surface and must not be treated as the production public frontend renderer.

## Frontend Candidate Matrix

| Candidate | Classification | Evidence | Audit Rule |
|---|---|---|---|
| `/Users/rainie/Desktop/GitHub/fap-web` | `primary_public_runtime` | `package.json` declares the Next app and runtime scripts; `next.config.mjs` uses standalone output; `ecosystem.config.cjs` points PM2 at `.next/standalone/server.js`. | Treat as the public frontend source candidate for route, renderer, SEO, llms, sitemap, and public runtime audits. |
| `/Users/rainie/Desktop/GitHub/fap-web` PM2 deploy config | `deploy_source` | `scripts/deploy_web_pm2.sh` deploys into `/opt/apps/fap-web` and probes `https://fermatmind.com`; `ecosystem.config.cjs` defines the PM2 app. | Use as deployment-source evidence only. Do not modify deployment behavior in PR-PRAC-01. |
| `/Users/rainie/Desktop/GitHub/fap-web` Next rewrites | `backend_proxy` | `next.config.mjs` proxies `/api/v0.3/*` and selected `/api/v0.5/*` public contracts to the backend API origin. | Treat as backend contract bridge evidence. It does not make frontend the owner of backend truth. |
| `/Users/rainie/Desktop/GitHub/fap-api/fap-web` | `skeleton_or_stale` | Nested copy has `app/sitemap.ts` and `app/robots.ts` only in the audited surface. | Do not use as public page-renderer evidence. Do not infer route coverage from it. |
| Other local `fap-web` copies or worktrees | `unrelated_worktree` | Local copies such as `fap-api-deploy/fap-web`, `fap-api-payfix/fap-web`, and task-specific worktrees may exist. | Do not treat as production source unless explicitly pinned by deploy source evidence. |
| Any unpinned frontend source | `unknown` | No verified package, build, deploy, or route evidence. | Exclude from production runtime judgments until classified. |

## Deployment Source Matrix

| Deployment Surface | Source Evidence | Runtime Meaning |
|---|---|---|
| Next app identity | `fap-web/package.json` | Buildable frontend application source. |
| Next standalone output | `fap-web/next.config.mjs` | Production-like server artifact is generated by Next standalone build. |
| PM2 app process | `fap-web/ecosystem.config.cjs` | Runtime process points to `.next/standalone/server.js`. |
| Server deploy script | `fap-web/scripts/deploy_web_pm2.sh` | Deployment flow checks out a git branch, installs dependencies, builds, syncs standalone assets, reloads PM2, and probes local/public health. |
| Backend API proxy | `fap-web/next.config.mjs` | Public frontend routes can consume backend contracts through proxy rewrites. |
| Nested skeleton sitemap | `fap-api/fap-web/app/sitemap.ts` | Static skeleton artifact only; not a public page renderer. |
| Nested skeleton robots | `fap-api/fap-web/app/robots.ts` | Static skeleton artifact only; not a public page renderer. |

## Route Ownership Matrix

| Route Family | Runtime Source Owner | Backend/CMS Truth Owner | Notes |
|---|---|---|---|
| Public pages under `app/(localized)` | `primary_public_runtime` | Backend/CMS where API surfaces exist | Renderer evidence must come from `/Users/rainie/Desktop/GitHub/fap-web/app`. |
| `/api/v0.3/*` proxy | `backend_proxy` | `fap-api` backend | Frontend is transport bridge only. |
| Selected `/api/v0.5/*` proxy | `backend_proxy` | `fap-api` backend/CMS/public-surface services | Includes articles, topics, personality, career, landing surfaces, and sitemap source proxies. |
| `/sitemap.xml` generation | `primary_public_runtime` with backend-source consumption | Backend/CMS for URL authority where source exists | Static sitemap layers must be audited separately for drift. |
| `/llms.txt` and `/llms-full.txt` | `primary_public_runtime` with backend/CMS inputs | Backend/CMS where source exists | Frontend fallbacks are not final authority. |
| Protected private flows | `primary_public_runtime` renderer with noindex policy | Backend auth/report/order services | Must not be inferred from nested skeleton files. |

## Skeleton/Stale Copy Warning

The nested `fap-api/fap-web` path is explicitly non-authoritative for public product runtime audits. It can provide evidence that stale or skeleton discoverability files exist, but it cannot prove public route coverage, renderer behavior, JSON-LD behavior, CMS consumption, paywall behavior, or graph/recommendation runtime.

Audit rule:

```text
Do not treat nested fap-api/fap-web as public frontend runtime.
Only classify it as skeleton_or_stale unless future deployment evidence explicitly pins it as a production source.
```

## Evidence References

- `fap-web/package.json`
- `fap-web/next.config.mjs`
- `fap-web/ecosystem.config.cjs`
- `fap-web/scripts/deploy_web_pm2.sh`
- `fap-api/fap-web/app/sitemap.ts`
- `fap-api/fap-web/app/robots.ts`
