# 九型人格完整结果页：独立前端设计预览

任务：FM-ENNEAGRAM-FRONTEND-PREVIEW-01。

这是七章结果页的可运行设计资料，不是正式报告、题型实现或可上线内容包。HTML/CSS/JS 自包含；不被 Next.js 路由、public 目录或运行时代码引用。

从仓库根目录启动：

```sh
python3 -m http.server 3106 --bind 127.0.0.1 --directory docs/design/enneagram-result-preview
```

打开 http://127.0.0.1:3106/ 。如端口已被已有预览占用，请使用该预览，或明确选择另一个本地端口。

## 覆盖

七章、28 个子板块：1.1—1.4 与 7.2—7.4 为七个共用模块；其余 21 项随全局候选切换。8、3、9 各有完整正文，合计 70 个内容实例；不是同屏 70 项。

包含三组两型对照与共同情境、每型七环节循环、九层结构样稿、五条完整行动建议、七天计划、事件与反例记录、复制和清空。候选切换不改变分布、候选状态、所选行动或记录；已选行动记录原候选和资产版本。

所有数值都是布局样例，所有正文都是未正式审核的原创预览材料。E105／FC144 标签情境不显示未知量尺分数。局部缺失和更多候选不会复用其他类型正文。

## 数据与发布边界

记录只存在本页内存，刷新清空，不读取旧版浏览器存储。没有业务 API、计分、账号、支付或分析写入；邮箱找回只保留禁用的外观展示。理论链接是外部阅读入口，不代表费马题本已有对应验证。

本目录作为设计资料版本化，不接入正式结果路由。内容审核、真实后端契约接入和生产上线需另行实施。

## 验证范围

可直接运行 `node --check docs/design/enneagram-result-preview/app.js` 和相同目录的 `content.js`。

本地预览已进行 8／3／9 切换、对照、九层展开、行动与记录保持、复制及失败提示、刷新清空、缺失情境和 1440／1024／390／360 CSS 视口检查。200% CSS 放大重排已检查；原生浏览器菜单 200%、真实触屏设备、读屏器和打印产物未验证。

这些是前端预览验收，不等于正式理论、心理测量或生产验收。截图和本地验收明细保留在任务预览目录，不作为应用资产提交。
