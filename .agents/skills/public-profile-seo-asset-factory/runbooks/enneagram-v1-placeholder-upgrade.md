# Enneagram Authority V2 116-Page Runbook

The filename is retained for compatibility. This runbook replaces the obsolete placeholder-only cohort instructions.

## Scope

Govern existing targets only:

- 1 hub, 3 centers, 9 core types, 18 wings, and 27 instinctual subtypes per locale.
- `en` and `zh-CN` are independent drafts.

Total: 58 identities and 116 locale pages.

## Steps

1. Load the backend-authoritative 58-identity target registry; do not invent routes.
2. Load the V2 source ledger and page-level claim maps; fail closed on missing evidence.
3. Open an isolated working revision for each target without changing the published primary fingerprint or public revision pointer.
4. Draft `en` and `zh-CN` independently from the same approved claim map.
5. Run schema, private-boundary, duplicate, framework, and bilingual-independence gates.
6. Record model/agent QA separately and keep the asset `pending_manual_review`; model review is not human review.
7. Hand off only draft-revision evidence. Publish, indexability, sitemap, llms, promotion, and rollback remain separate backend-authorized gates.

## Forbidden Expansion

- No 54 wing × instinct matrix.
- No Tritype.
- No new URL generation.
