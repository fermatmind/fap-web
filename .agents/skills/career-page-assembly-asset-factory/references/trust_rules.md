# Career Page Assembly Trust Rules

Block missing dependency fallback copy, raw enum leakage, source ID exposure, internal lineage exposure, and unsupported JSON-LD or schema expansion.

Trust audit must fail rows with fabricated facts, unsupported proxy use, raw internal metadata, missing source boundaries, or reader-facing claims that exceed the evidence.
