# EU Context Rules

Default EU status is `macro_context_only`.

Allowed statuses:

- `macro_context_only`
- `occupation_specific`
- `unavailable`

Do not state unified European occupational wages or EU-wide median salary unless an occupation-specific source is captured and audited.

Eurostat labour-market earnings pages are macro context, not a direct occupation salary source.
