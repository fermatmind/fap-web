# Goal Run Block Evidence

Goal: Run evidence collection for one authorized block batch.

Use the block-specific factory. Evidence must precede synthesis/assets. Stop on blocked rows. Do not self-declare PASS.
