# Goal Freeze Baseline

Goal: Freeze a PASS block baseline.

Copy PASS artifacts into a new baseline directory, write SHA manifest and freeze report, then stop. Do not create the next content batch unless explicitly requested.
