# Browser Capture Checklist

Use Browser, Chrome, or Computer Use only when static collection cannot verify the source.

Capture:

- URL
- visible source title
- visible fact/snippet
- date or source version when visible
- screenshot or copied snippet when needed
- reason static collection was insufficient

Do not treat search snippets as final evidence unless the block rules explicitly allow query-level evidence.

