# Goal: Run Career Content Operator Loop

Use `career-content-asset-factory`.

Run the operator loop continuously under this concrete scoped `/goal`. Continue evidence, repair, freeze, independent W9/QA, and an eligible trusted V2 promotion dispatch through their machine gates. Use dry-run only when the goal requests planning-only output.

Stop only on a named machine-gate failure, direct-action prohibition, separately controlled scope, or exhausted repair loop. Repair a V2 `BLOCKED` verdict in the same Producer PR; do not create approval-only, reset, refreeze, or status-acceptance work.
