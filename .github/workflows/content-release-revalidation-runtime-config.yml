name: Content Release Revalidation Runtime Config

on:
  workflow_dispatch:
    inputs:
      mode:
        description: Read-only preflight or exact-authorized apply
        required: true
        type: choice
        options:
          - preflight
          - apply
      expected_control_plane_sha:
        description: Exact latest main SHA containing this workflow
        required: true
        type: string
      expected_frontend_sha:
        description: Exact currently active frontend SHA
        required: true
        type: string
      preflight_run_id:
        description: Apply-only immutable successful preflight run id
        required: false
        type: string
      preflight_run_attempt:
        description: Apply-only immutable successful preflight run attempt
        required: false
        type: string
      expected_env_sha256:
        description: Apply-only exact environment file SHA256 from preflight
        required: false
        type: string
      expected_runtime_fingerprint_sha256:
        description: Apply-only exact runtime fingerprint from preflight
        required: false
        type: string
      expected_credential_bundle_sha256:
        description: Apply-only exact source credential bundle SHA256 from preflight
        required: false
        type: string
      authorization_phrase:
        description: Apply-only exact authorization phrase
        required: false
        type: string

permissions:
  actions: read
  contents: read

concurrency:
  group: fap-web-production-mutation
  cancel-in-progress: false

jobs:
  runtime-config:
    name: Preflight or apply content-release runtime config
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment:
      name: production
    env:
      MODE: ${{ inputs.mode }}
      EXPECTED_CONTROL_PLANE_SHA: ${{ inputs.expected_control_plane_sha }}
      EXPECTED_FRONTEND_SHA: ${{ inputs.expected_frontend_sha }}
      PREFLIGHT_RUN_ID: ${{ inputs.preflight_run_id }}
      PREFLIGHT_RUN_ATTEMPT: ${{ inputs.preflight_run_attempt }}
      EXPECTED_ENV_SHA256: ${{ inputs.expected_env_sha256 }}
      EXPECTED_RUNTIME_FINGERPRINT_SHA256: ${{ inputs.expected_runtime_fingerprint_sha256 }}
      EXPECTED_CREDENTIAL_BUNDLE_SHA256: ${{ inputs.expected_credential_bundle_sha256 }}
      AUTHORIZATION_PHRASE: ${{ inputs.authorization_phrase }}
      APP_DIR: /opt/apps/fap-web
      APP_NAME: fap-web
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Validate exact control-plane inputs
        shell: bash
        run: |
          set -euo pipefail
          test "$MODE" = preflight -o "$MODE" = apply
          test "$EXPECTED_CONTROL_PLANE_SHA" = "$GITHUB_SHA"
          [[ "$EXPECTED_CONTROL_PLANE_SHA" =~ ^[0-9a-f]{40}$ ]]
          [[ "$EXPECTED_FRONTEND_SHA" =~ ^[0-9a-f]{40}$ ]]
          if [ "$MODE" = preflight ]; then
            test -z "$AUTHORIZATION_PHRASE"
          else
            [[ "$PREFLIGHT_RUN_ID" =~ ^[0-9]+$ ]]
            [[ "$PREFLIGHT_RUN_ATTEMPT" =~ ^[0-9]+$ ]]
            [[ "$EXPECTED_ENV_SHA256" =~ ^[0-9a-f]{64}$ ]]
            [[ "$EXPECTED_RUNTIME_FINGERPRINT_SHA256" =~ ^[0-9a-f]{64}$ ]]
            [[ "$EXPECTED_CREDENTIAL_BUNDLE_SHA256" =~ ^[0-9a-f]{64}$ ]]
            test -n "$AUTHORIZATION_PHRASE"
          fi

      - name: Checkout exact control plane
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          ref: ${{ inputs.expected_control_plane_sha }}
          fetch-depth: 0

      - name: Confirm control plane remains latest main
        shell: bash
        run: |
          set -euo pipefail
          git fetch --no-tags origin main:refs/remotes/origin/main
          test "$(git rev-parse HEAD)" = "$EXPECTED_CONTROL_PLANE_SHA"
          test "$(git rev-parse origin/main)" = "$EXPECTED_CONTROL_PLANE_SHA"

      - name: Verify required checks are green
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8
        env:
          INPUT_CONTROL_PLANE_SHA: ${{ inputs.expected_control_plane_sha }}
        with:
          script: |
            const sha = process.env.INPUT_CONTROL_PLANE_SHA || '';
            const required = [
              'build',
              'contracts',
              'verify-big5-contract-freeze',
              'verify-enneagram-contract-freeze',
            ];
            const runs = await github.paginate(github.rest.checks.listForRef, {
              ...context.repo,
              ref: sha,
              per_page: 100,
            });
            for (const name of required) {
              const run = runs
                .filter((item) => item.name === name)
                .sort((a, b) => Number(b.id) - Number(a.id))[0];
              if (!run || run.status !== 'completed' || run.conclusion !== 'success') {
                core.setFailed(`Required check is not green: ${name}.`);
                return;
              }
            }

      - name: Validate immutable preflight receipt
        if: inputs.mode == 'apply'
        shell: bash
        run: |
          set -euo pipefail
          run_json="$(gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${PREFLIGHT_RUN_ID}")"
          jq -e \
            --arg sha "$EXPECTED_CONTROL_PLANE_SHA" \
            --argjson attempt "$PREFLIGHT_RUN_ATTEMPT" \
            '.status == "completed"
             and .conclusion == "success"
             and .event == "workflow_dispatch"
             and .head_sha == $sha
             and .run_attempt == $attempt' <<<"$run_json" >/dev/null
          test "$(jq -r '.path' <<<"$run_json")" = \
            ".github/workflows/content-release-revalidation-runtime-config.yml"
          preflight_dir="$(mktemp -d)"
          gh run download "$PREFLIGHT_RUN_ID" \
            --name "content-release-runtime-config-preflight-${PREFLIGHT_RUN_ID}-${PREFLIGHT_RUN_ATTEMPT}" \
            --dir "$preflight_dir"
          receipt="$preflight_dir/content-release-revalidation-runtime-config.json"
          test -f "$receipt"
          jq -e \
            --arg control "$EXPECTED_CONTROL_PLANE_SHA" \
            --arg frontend "$EXPECTED_FRONTEND_SHA" \
            --arg env_sha "$EXPECTED_ENV_SHA256" \
            --arg runtime_sha "$EXPECTED_RUNTIME_FINGERPRINT_SHA256" \
            --arg credential_sha "$EXPECTED_CREDENTIAL_BUNDLE_SHA256" \
            '.mode == "preflight"
             and .ok == true
             and .apply_ready == true
             and .production_write_execution == false
             and .writes_committed == false
             and .control_plane_sha == $control
             and .active_frontend_sha == $frontend
             and .env_sha256 == $env_sha
             and .runtime_fingerprint_sha256 == $runtime_sha
             and .source_credential_bundle_sha256 == $credential_sha' "$receipt" >/dev/null

      - name: Set up SSH
        uses: webfactory/ssh-agent@e83874834305fe9a4a2997156cb26c5de65a8555 # v0.10.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Install pinned SSH known hosts
        shell: bash
        env:
          SSH_KNOWN_HOSTS: ${{ secrets.SSH_KNOWN_HOSTS }}
        run: |
          set -euo pipefail
          test -n "$SSH_KNOWN_HOSTS"
          mkdir -p ~/.ssh
          printf '%s\n' "$SSH_KNOWN_HOSTS" > ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts

      - name: Run bounded runtime config control
        id: control
        shell: bash
        env:
          DEPLOY_HOST: ${{ secrets.WEB_NODE1_DEPLOY_HOST }}
          DEPLOY_USER: ${{ secrets.WEB_NODE1_DEPLOY_USER }}
          DEPLOY_PORT: ${{ secrets.WEB_NODE1_DEPLOY_PORT }}
          CONTENT_RELEASE_REVALIDATE_SECRET: ${{ secrets.CONTENT_RELEASE_REVALIDATE_SECRET }}
          CONTENT_RELEASE_REVALIDATE_REDIS_URL: ${{ secrets.CONTENT_RELEASE_REVALIDATE_REDIS_URL }}
          CONTENT_RELEASE_REVALIDATE_REDIS_TOKEN: ${{ secrets.CONTENT_RELEASE_REVALIDATE_REDIS_TOKEN }}
        run: |
          set -euo pipefail
          test -n "$DEPLOY_HOST"
          test -n "$DEPLOY_USER"
          test -n "$DEPLOY_PORT"
          receipt="$RUNNER_TEMP/content-release-revalidation-runtime-config.json"
          set +e
          {
            printf 'export MODE=%q\n' "$MODE"
            printf 'export APP_DIR=%q\n' "$APP_DIR"
            printf 'export APP_NAME=%q\n' "$APP_NAME"
            printf 'export EXPECTED_CONTROL_PLANE_SHA=%q\n' "$EXPECTED_CONTROL_PLANE_SHA"
            printf 'export EXPECTED_FRONTEND_SHA=%q\n' "$EXPECTED_FRONTEND_SHA"
            printf 'export PREFLIGHT_RUN_ID=%q\n' "$PREFLIGHT_RUN_ID"
            printf 'export PREFLIGHT_RUN_ATTEMPT=%q\n' "$PREFLIGHT_RUN_ATTEMPT"
            printf 'export EXPECTED_ENV_SHA256=%q\n' "$EXPECTED_ENV_SHA256"
            printf 'export EXPECTED_RUNTIME_FINGERPRINT_SHA256=%q\n' "$EXPECTED_RUNTIME_FINGERPRINT_SHA256"
            printf 'export EXPECTED_CREDENTIAL_BUNDLE_SHA256=%q\n' "$EXPECTED_CREDENTIAL_BUNDLE_SHA256"
            printf 'export AUTHORIZATION_PHRASE=%q\n' "$AUTHORIZATION_PHRASE"
            printf 'export CONTENT_RELEASE_REVALIDATE_SECRET=%q\n' "$CONTENT_RELEASE_REVALIDATE_SECRET"
            printf 'export CONTENT_RELEASE_REVALIDATE_REDIS_URL=%q\n' "$CONTENT_RELEASE_REVALIDATE_REDIS_URL"
            printf 'export CONTENT_RELEASE_REVALIDATE_REDIS_TOKEN=%q\n' "$CONTENT_RELEASE_REVALIDATE_REDIS_TOKEN"
            cat scripts/content_release_revalidation_runtime_config.sh
          } | ssh \
            -o BatchMode=yes \
            -o StrictHostKeyChecking=yes \
            -o ConnectTimeout=10 \
            -p "$DEPLOY_PORT" \
            "$DEPLOY_USER@$DEPLOY_HOST" \
            "bash -s" \
            > "$receipt"
          runner_status=$?
          set -e
          cp "$receipt" content-release-revalidation-runtime-config.json
          jq -e '.secret_values_output == false' "$receipt" >/dev/null
          if [ "$runner_status" -ne 0 ]; then
            jq -e '.ok == false' "$receipt" >/dev/null
            exit "$runner_status"
          fi
          if [ "$MODE" = preflight ]; then
            jq -e \
              '.mode == "preflight"
               and .ok == true
               and .production_write_execution == false
               and .writes_committed == false
               and .application_deploy == false
               and .cache_revalidation == false' "$receipt" >/dev/null
          else
            jq -e \
              '.mode == "apply"
               and .ok == true
               and .status == "PASS_RUNTIME_CONFIG_CONVERGED"
               and .writes_committed == true
               and .runtime_setting_write_count == 3
               and .pm2_reload_committed == true
               and .application_deploy == false
               and .cache_revalidation == false' "$receipt" >/dev/null
          fi

      - name: Upload preflight receipt
        if: always() && inputs.mode == 'preflight'
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: content-release-runtime-config-preflight-${{ github.run_id }}-${{ github.run_attempt }}
          path: content-release-revalidation-runtime-config.json
          retention-days: 14
          if-no-files-found: error

      - name: Upload apply receipt
        if: always() && inputs.mode == 'apply'
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: content-release-runtime-config-apply-${{ github.run_id }}-${{ github.run_attempt }}
          path: content-release-revalidation-runtime-config.json
          retention-days: 14
          if-no-files-found: error
